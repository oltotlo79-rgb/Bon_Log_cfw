/**
 * @file お知らせ管理ページのローディング画面
 */

import { LoadingScreen } from '@/components/common/LoadingScreen'

export default function AnnouncementsLoading() {
  return <LoadingScreen message="お知らせ一覧を読み込んでいます..." />
}
